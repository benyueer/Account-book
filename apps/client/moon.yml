language: typescript
type: application

tasks:
  dev:
    command: vite
    local: true
  build:
    command: vite build
    inputs:
      - 'src/**/*'
      - vite.config.ts
    outputs:
      - dist
  lint:
    command: eslint .
