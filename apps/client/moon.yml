language: typescript
type: application

tasks:
  dev:
    command: pnpm run dev
    local: true
  build:
    command: pnpm run build
    inputs:
      - 'src/**/*'
      - vite.config.ts
    outputs:
      - dist
  lint:
    command: pnpm run lint
