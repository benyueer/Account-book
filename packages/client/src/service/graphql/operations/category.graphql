query getConsumptionType($familyId: Float) {

  getConsumptionType(familyId: $familyId) {
    id
    pid
    baseType
    name
    icon
    children {
      id
      pid
      baseType
      name
    }
  }
}